<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.WeatherApp22" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Основные цвета темы -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>

        <!-- Цвет статус-бара и навигационной панели -->
        <item name="android:statusBarColor">@color/card_blue_bg</item>
        <item name="android:navigationBarColor">@color/card_blue_bg</item>

        <!-- Белый текст и иконки на статус-баре -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Настройка для TabLayout -->
        <item name="tabTextColor">@android:color/white</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabIndicatorColor">@android:color/white</item>
    </style>
</resources>